<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>keyfull</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>KeyFull</name>
    <description>KeyFull Project</description>

	<dependencies>
	    <dependency>
	        <groupId>net.java.dev.jna</groupId>
	        <artifactId>jna</artifactId>
	        <version>5.13.0</version>  <!-- Ensure it's the latest version -->
	    </dependency>
	    <dependency>
	        <groupId>net.java.dev.jna</groupId>
	        <artifactId>jna-platform</artifactId>
	        <version>5.13.0</version>  <!-- Must match jna version -->
	    </dependency>
	</dependencies>

    <build>
      <plugins>
          <!-- Compiler Plugin -->
          <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-compiler-plugin</artifactId>
              <version>3.8.1</version>
              <configuration>
                  <source>1.8</source> <!-- or 11, 17, depending on your Java version -->
                  <target>1.8</target> <!-- or 11, 17, depending on your Java version -->
              </configuration>
          </plugin>

          <!-- Exec Plugin to run the application -->
          <plugin>
              <groupId>org.codehaus.mojo</groupId>
              <artifactId>exec-maven-plugin</artifactId>
              <version>3.1.0</version>
              <executions>
                  <execution>
                      <goals>
                          <goal>java</goal>
                      </goals>
                  </execution>
              </executions>
              <configuration>
                  <mainClass>com.keyfull.Select</mainClass> <!-- Correct path to the main class -->
              </configuration>
          </plugin>
      </plugins>
    </build>

</project>

